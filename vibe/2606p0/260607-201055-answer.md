# 260607-201055 执行记录

## 任务结论

已确认本次安装报“软件包似乎无效”的高概率原因在 fork 专用构建流水线：旧版 `.github/workflows/fork-build-apk.yml` 在缺少 `KEYSTORE_BASE64 / SIGNING_KEY_ALIAS / SIGNING_KEY_PASSWORD / SIGNING_STORE_PASSWORD` 任一 secret 时，会继续产出未签名 release APK。Android 不能安装 unsigned APK。

本轮已修复为：

- secrets 齐全：继续构建 `assembleRelease`，发布 `gpslogger-travel-release-...apk`。
- secrets 缺失：不再构建/发布 unsigned release，改为构建 `assembleDebug`，发布 `gpslogger-travel-debug-...apk`，作为可安装的测试包。
- Release 发布前清理旧 workflow 可能留下的 `-unsigned` 资产，避免用户误下载旧无效包。
- 文档补充：Actions artifact 下载的是 zip，需要解压后安装里面的 `.apk`；Release 页面下载的 `.apk` 才能直接安装。

## 依据来源

- 项目使用 Android application 插件：`gpslogger/build.gradle:18`；包名为 `com.mendhak.gpslogger`，当前版本 `versionName "135"`：`gpslogger/build.gradle:42-49`。
- 当前 `debug` buildType 存在且 `debuggable true`：`gpslogger/build.gradle:57-63`。
- 当前 `release` buildType 只有在 Gradle 属性或环境变量提供签名信息时才绑定 `signingConfig signingConfigs.release`：`gpslogger/build.gradle:82-113`。
- 官方 Android Gradle API 文档说明默认 `debug` build type 会用 debug key 签 APK，而 `release` build type 需要配置 release key 后用于分发：https://developer.android.com/reference/tools/gradle-api/9.3/com/android/build/api/dsl/ApplicationBuildType 。
- 官方 Android app signing 文档说明 debug certificate 由 Android SDK tools 创建，不适合应用商店正式发布；同一应用升级安装需要签名证书匹配：https://developer.android.com/studio/publish/app-signing 。
- fork workflow 现在在缺少签名 secrets 时明确走 `assembleDebug` fallback：`.github/workflows/fork-build-apk.yml:137-172`。
- workflow 现在按 `have_keystore` 选择 release 或 debug 输出目录，并把产物命名为 `gpslogger-travel-${BUILD_KIND}-...apk`：`.github/workflows/fork-build-apk.yml:174-219`。
- workflow 现在会清理旧版 `-unsigned` Release 资产：`.github/workflows/fork-build-apk.yml:238-250`。
- workflow Release 文案现在显示 `build_kind` 与 `signing_status`，并提示 debug-signed 不是正式发布包：`.github/workflows/fork-build-apk.yml:264-282`。
- workflow README 现在说明 Release 直接下载 APK、Actions artifact 需要先解压：`.github/workflows/README.md:25-33`。
- workflow README 现在把“软件包似乎无效”的排查点写入文档：`.github/workflows/README.md:70-73`。

## 方案取舍

- 不选择“继续发布 unsigned APK 并提示用户不要安装”。原因：用户最容易从 Release 页面直接下载最新资产，保留 unsigned 资产会继续制造同类安装失败。
- 不在仓库里内置公共 release keystore。原因：公共 keystore 会让测试包看起来像 release 包，但签名私钥公开，不适合作为正式发布身份。
- 选择 debug-signed fallback。原因：无需 secrets，Android 可安装，满足个人 fork 自测；缺点是 debug 签名不适合正式分发，且不同签名的旧版本不能覆盖安装。文档已要求正式 release 配置 keystore secrets。

## 已实施改动

### `.github/workflows/fork-build-apk.yml`

- 修改 workflow 注释和 `workflow_dispatch` 描述，明确无 secrets 时是 `debug fallback`，不是 unsigned release。
- `build-release` job 中：
  - secrets 齐全时仍执行 `./gradlew :gpslogger:assembleRelease --no-daemon --stacktrace`。
  - secrets 缺失时执行 `./gradlew :gpslogger:assembleDebug --no-daemon --stacktrace`。
- `Locate packaged APK` 根据 `have_keystore` 选择：
  - `gpslogger/build/outputs/apk/release`，产物类型 `release`，签名状态 `release-signed`。
  - `gpslogger/build/outputs/apk/debug`，产物类型 `debug`，签名状态 `debug-signed`。
- 增加 `Remove old unsigned release assets`，在 tag/nightly Release 发布前删除旧 `-unsigned` 资产。
- Release body 增加构建类型、签名状态和 debug-signed 安装注意事项。

### `.github/workflows/README.md`

- 更新构建产物说明：signed release 与 debug-signed fallback 都可安装，旧 `-unsigned.apk` 不可安装。
- 明确 Actions artifact 是 zip，需解压后安装 `.apk`。
- 明确没有配置 keystore secrets 时应下载 `gpslogger-travel-debug-...apk`。
- 增加“软件包似乎无效”排错说明。

## 副作用与兼容性

- 已有签名 secrets 的正式 release 链路不变，仍走 `assembleRelease` 和 release keystore。
- 未配置 secrets 的 tag/nightly Release 将不再出现 `gpslogger-travel-release-...-unsigned.apk`，而是出现 `gpslogger-travel-debug-...apk`。
- debug-signed fallback 只能作为测试包。若手机已安装过不同签名的 GPSLogger，需要先卸载旧版本；若需要后续稳定覆盖安装，应配置固定 keystore secrets。
- 旧 nightly Release 里如果已经存在 `-unsigned` 资产，新 workflow 下次运行会尝试删除这些资产；如果没有既有 Release，该清理步骤会直接退出，不阻塞创建 Release。

## 验证记录

已执行并通过：

```sh
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/fork-build-apk.yml', encoding='utf-8')); print('yaml ok')"
```

结果：`yaml ok`。

已执行并通过：

```sh
git diff --check -- .github/workflows/README.md .github/workflows/fork-build-apk.yml
```

结果：无输出，未发现 diff 空白错误。

已执行残留检查：

```sh
rg -n -- "-unsigned|unsigned APK|Assemble release APK \(unsigned\)|release APK 会以|软件包似乎无效" .github/workflows/README.md .github/workflows/fork-build-apk.yml vibe/2606p0/260607-201055-prompt.md
```

结果：只剩以下预期内容：

- prompt 原始问题里的“软件包似乎无效”。
- workflow/README 中用于解释旧 `-unsigned.apk` 不可安装、清理旧资产、排错提示的文字。
- 不再存在 `Assemble release APK (unsigned)` 步骤。

尝试执行本地 APK 构建：

```sh
./gradlew :gpslogger:assembleDebug --no-daemon --stacktrace
```

结果未能在本机完成，错误为：`JAVA_HOME is not set and no 'java' command could be found in your PATH.` 随后检查 `command -v java`、`/usr/lib/jvm`、`/opt`，当前环境没有可用 JDK。GitHub Actions workflow 自身仍通过 `actions/setup-java@v4` 配置 Temurin JDK 17：`.github/workflows/fork-build-apk.yml:66-72`、`:127-132`。

## 待确认

- 需要 push 到 GitHub 后观察一次 `Fork Build APK` workflow 实际运行结果，确认 Release 页面出现的是：
  - 有 secrets：`gpslogger-travel-release-...apk`。
  - 无 secrets：`gpslogger-travel-debug-...apk`。
- 如果用户希望以后不用卸载即可覆盖安装每个测试包，需要配置固定 keystore secrets；否则不同构建环境生成的 debug key 可能导致 Android 拒绝覆盖安装。


## 追加修正：流水线成功但 Releases 没有新 APK

用户反馈：流水线没有报错，但是 GitHub Releases 没有生成新的 APK。

### 原因确认

旧修复后的 workflow 仍保留一个行为：`workflow_dispatch + build_release=true` 只上传 Actions artifact，不创建 GitHub Release。依据是 `.github/workflows/fork-build-apk.yml` 原注释和 `Create GitHub Release` 步骤条件只覆盖 `tag/nightly`，不覆盖手动触发模式。

因此如果手动运行 workflow，Actions 会成功，artifact 区也会有 APK，但 Releases 页面不会出现新 APK。

### 本次改动

- 手动触发 `workflow_dispatch` 且选择 `build_release=true` 时，现在会进入 `manual` 发布模式：`.github/workflows/fork-build-apk.yml:180-195`。
- `manual` 模式会使用固定 tag `manual` 与 Release 名称 `Manual Build`：`.github/workflows/fork-build-apk.yml:191-195`。
- master push 的 `nightly` 与手动触发的 `manual` 都会移动浮动 tag 到当前提交，保证 Release 指向这次 workflow 的 commit：`.github/workflows/fork-build-apk.yml:231-236`。
- `Create GitHub Release` 现在覆盖 `tag/nightly/manual` 三种模式，`manual` 会作为 prerelease 发布：`.github/workflows/fork-build-apk.yml:266-276`。
- `nightly/manual` 固定 Release 上传前会删除旧的 `gpslogger-travel-*` APK 和 SHA256 资产，避免用户在 Release 页面误下历史包：`.github/workflows/fork-build-apk.yml:252-264`。
- README 已同步说明手动触发会刷新固定的 `manual` 预发布 Release：`.github/workflows/README.md:14-23`、`:60-68`。

### 追加验证

已执行并通过：

```sh
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/fork-build-apk.yml', encoding='utf-8')); print('yaml ok')"
```

结果：`yaml ok`。

已执行并通过：

```sh
git diff --check -- .github/workflows/fork-build-apk.yml .github/workflows/README.md
```

结果：无输出。

本机未安装 `actionlint`；本轮未运行 GitHub Actions 远端 workflow。下一次 push 后，手动运行 `Fork Build APK` 并选择 `build_release=true`，Releases 页面应出现或刷新 `Manual Build`，其中包含 `gpslogger-travel-debug-...manual-<sha>.apk` 或 `gpslogger-travel-release-...manual-<sha>.apk`。

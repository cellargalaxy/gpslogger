# 执行流程

> 流程冲突时以「任务 Prompt」为准。
> 关键结论/方案取舍/验证结果统一记录至`260607-162156-answer.md`(`prompt.md`路径相同)。

## 步骤

- 背景调研
    - 研读文档，对齐背景
    - 研读代码/IDL/配置，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：改动最小/复用优先；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/IDL/表结构/日志/SQL等明确来源；**严厉禁止猜测业务逻辑/捏造接口字段**。
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引。
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补。
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用。
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方。
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现。

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
    - [260607-151406-answer.md](260607-151406-answer.md)
        - 只用于参考，这个方案也不一定正确，所涉及的相关内容你都需要独立复核
        - 你输出的answer文件的分隔也不需要参考本文档
- 代码风格：
- 交付路径：

## 具体任务

+ 本代码仓库fork自https://github.com/mendhak/gpslogger
+ 我想进行一些改造，希望能改造为更加适合与旅游徒步使用，下面是我的一些想法和诉求
    + 先帮我完善需求的设计思路，先不需要管代码的细节，输出完备详细的需求方案到`answer.md`
    + 下面提到的这些功能，可能官方现状已经有，只是我没找到如何进行配置。如果官方已经有的话则不需要开发了，你告诉我如何进行配置即可
    + 虽然下面有些提问，但本质不是需要你来回答我，而是你在输出需求文档时需要注意的要点
+ 我是用的是Custom URL
+ 如果处于联网状态，点信息只通过用户在app中配置了的选项进行发送
    + 这点应该跟官方现状一致
+ 如果处于断网状态，点信息缓存到手机本地磁盘，如果恢复联网再补推这些点
    + 补推的点的时间之类的信息，需要与记录的时候一致，并不是将补推份的点都记录在恢复联网的时间地点中
+ 添加一个设置项，是一个时间范围
    + 记录到的点，除了会发送到用户在app中配置选择
    + 还会缓存在本地磁盘一段时间
    + 方便用户在断网状态下，查询过去这段时间的轨迹
+ app新增一个展示视图，展示的是地图，在地图上展示过去一段时间里的轨迹
    + 新增一个设置项，是一个时间段
        + 枚举有：五分钟/十分钟/十五分钟/三十分钟/60分钟
    + 将轨迹按时间段进行切割，标注为不同颜色
    + 例如时间段选择的是15分钟，轨迹从13:12到13:33
    + 则13:12~13:15一段，13:15~13:30一段，13:30~13:33一段
+ 断网时每个点会进入 WorkManager，WorkManager的数据会持久化吗，如果杀app会不会导致数据丢失。
    + 能接受杀app丢失最近一两个点的数据，但是不能接受大部分/全部等待联网上传的点都丢失
+ 为什么会需要「批量补发 CSV 每一行」，app一直在实时记录，断网就缓存，上面说到「断网后仍保留每个点等待补推」那不就已经会补发了吗，为什么会有CSV。
+ 新增 轨迹地图。
+ 本地轨迹缓存保留时间，需要能自动清除超过缓存时间的数据。
+ 地图 SDK，需要支持提前在有网情况下把地图数据缓存下载，在之后无网状态下查看。
+ 地图 SDK的具体选型还需要斟酌一下。首先需要开源、无 Google Play Services 依赖，但是我看osmdroid已经停止更新两年，代码仓库也归档没有维护了。
+ 需要有管理界面，用于管理地图数据缓存与清理，以知道已经缓存了哪些范围的地图，去触发地图缓存，清理无用缓存。
+ 本地轨迹保留时间的默认值采用 24 小时。
+ 如何不复杂，新增面向用户的队列管理视图也非常好。
+ 我是用的是Custom URL。
+ 配置的名称使用中文，我使用的是中文界面
    + 代码实际开发时，配置需要支持英文与中文。只是在answer文档中使用中文描述，方便的复核。 
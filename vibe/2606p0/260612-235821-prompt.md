# 执行流程

> 流程冲突时以「任务 Prompt」为准。
> 关键结论/方案取舍/验证结果统一记录至`260612-235821-answer.md`(`prompt.md`路径相同)。

## 步骤

- 背景调研
    - 研读文档，对齐背景
    - 研读代码/IDL/配置，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：改动最小/复用优先；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/IDL/表结构/日志/SQL等明确来源；**严厉禁止猜测业务逻辑/捏造接口字段**。
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引。
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补。
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用。
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方。
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现。

# 任务 Prompt

## 背景信息

- 现状文档：
    - [260607-162156-prompt.md](260607-162156-prompt.md)
    - [260607-162156-answer.md](260607-162156-answer.md)
- 现状代码：
- 现状描述：
- 需求文档：
- 代码风格：
- 交付路径：

## 具体任务

1. 继续优化功能
2. 轨迹地图视图中，地图中给当前位置添加一个图标，进行醒目展示
3. 轨迹地图视图中，现在地图底部会有各个时段的轨迹的颜色图例
    4. 将这些图例改为可选择
    5. 默认全选，显示全部的轨迹段
    6. 如果点击某一个图例，则地图只展示该图例的轨迹，其余轨迹的颜色给为半透明
        7. 注意，这是地图中的轨迹的颜色进行不透明与半透明变化，不是地图下面的图例
    8. 如果点击某一个图例，则只有该图例的颜色是不透明，其余图例颜色变成半透明，以表示选中了哪个图例
        9. 注意，这是图例本身的颜色进行不透明与半透明变化，不是地图中的轨迹
10. 轨迹地图视图中，现在地图顶部有三个按钮：刷新/定位到最新点/适应轨迹边界
    11. 这三个按钮保留，功能不变
    12. 新增一个开关按钮，如果开关打开，则在打开状态下，滑动操作地图所加载到的地图，都下载保持到本地，以在断网状态下也能加载
13. 配置中的旅行模式页面
    14. 去除「管理离线地图区域」的子页面
    15. 在「旅行模式」页面新增两个配置用于管理第12点中下载到本地的地图文件
        16. 展示已经缓存的地图文件的大小
        17. 清除全部缓存的地图文件，需要加一个二级确认，不能点击就立马删除了
    18. 去除「离线地图缓存上限」的配置，不限制缓存上限
19. 配置中的旅行模式页面
    20. 「情况本地轨迹缓存」需要加一个二级确认，不能点击就立马删除了
# 260619-143939 执行记录

## 依据来源

- 任务来源：`vibe/2606p1/260619-143939-prompt.md`，要求检查当前分支与 `mendhak_master` 的 diff，review 对 `mendhak_master` 既有代码文件的改动，并尽量改为非入侵方式。
- 当前分支：`feat_260619`。
- `mendhak_master`：`9db655b4417e66073e6a11ff09b16b8ac2b4d299`；`git merge-base HEAD mendhak_master` 也是该提交，说明当前分支从 `mendhak_master` 分出。
- 最终复核以 `git diff mendhak_master` 为准；`git status` 中 `.gitignore`、`CustomUrlRequest.java` 仍显示为工作区改动，是因为当前 HEAD 已包含旧改动，本轮工作区把它们恢复到了 `mendhak_master` 状态。

## Diff 复核结论

最终 `git diff --name-status mendhak_master` 中，仍修改的既有代码/配置文件为：

- `gpslogger/build.gradle`
- `gpslogger/src/main/java/com/mendhak/gpslogger/GpsLoggingService.java`
- `gpslogger/src/main/java/com/mendhak/gpslogger/GpsMainActivity.java`
- `gpslogger/src/main/java/com/mendhak/gpslogger/MainPreferenceActivity.java`
- `gpslogger/src/main/java/com/mendhak/gpslogger/common/Session.java`
- `gpslogger/src/main/java/com/mendhak/gpslogger/common/events/CommandEvents.java`
- `gpslogger/src/main/java/com/mendhak/gpslogger/loggers/FileLoggerFactory.java`
- `gpslogger/src/main/res/values/arrays.xml`
- `gpslogger/src/main/res/values/strings.xml`
- `gpslogger/src/main/res/values-zh-rCN/strings.xml`

## Review 与取舍

1. `CustomUrlRequest.java` 的 getter 改为 `public` 没有必要。当前调用方仍在同包 `loggers.customurl` 内；Outbox 相关跨包需求已在历史任务中删除。本轮已恢复为包可见，最终相对 `mendhak_master` 无 diff。
2. `.gitignore` 增加 `.claude` 不是业务代码，也不是实现 tracker/地图能力所需。本轮已恢复到 `mendhak_master` 原始内容，最终相对 `mendhak_master` 无 diff。
3. 本地轨迹缓存原先直接侵入 `GpsLoggingService.writeToFile()`。本轮改为在 `FileLoggerFactory.write()` 入口旁路写入 `TrackCacheRepository`，位置为 `FileLoggerFactory.java:102`-`:105`。这样仍处于“通过过滤后的有效点写入”同层，且在 `Session.clearDescription()` 之前执行，可保留 annotation/fileName/profile 上下文；失败仍由 Repository 内部吞掉，不影响 GPX/CSV/Custom URL 主链路。
4. `GpsLoggingService` 中 `GetNextPoint` / `isNextPointForced()` 相关改动仍需保留。依据是记录中点击第二个按钮时，原 `LogOnce` 会进入 single point 模式并在拿点后停止记录；要实现“记录中立即更新定位并触发保存/推送但不停止记录”，必须让该点绕过时间、最佳精度等待和距离过滤，见 `GpsLoggingService.java:1056`、`:1091`、`:1125`-`:1127`、`:1364`-`:1369`。
5. `GpsMainActivity` 的入口接线仍需保留：现有主界面视图由 `gps_main_views` 数组和 `loadFragmentView(position)` 的 hardcoded switch 驱动，轨迹地图必须增加 `case 5`，见 `GpsMainActivity.java:1060`-`:1064`；drawer 设置入口也由 hardcoded item id 分发，见 `GpsMainActivity.java:842`-`:846`、`:910`-`:912`。
6. `MainPreferenceActivity` 的 TRACKER 分支仍需保留。现有偏好页宿主通过 `PREFERENCE_FRAGMENTS` 字符串 switch 实例化 fragment，新增 tracker 设置页接入该宿主比新增 Activity 和 Manifest 注册更少改动，见 `MainPreferenceActivity.java:144`-`:148`。
7. `build.gradle` 的 MapLibre 依赖仍需保留。新增 `TrackMapViewFragment`、`MapLibreOfflineMapStore`、`OfflineMapManagerActivity` 直接引用 `org.maplibre.android.*` API，见依赖位置 `gpslogger/build.gradle:211`-`:213`。
8. `strings.xml`、`values-zh-rCN/strings.xml` 和 `arrays.xml` 的改动属于 UI 接线和文案资源。`arrays.xml:57`-`:64` 是现有 ActionBar view spinner 的数据源，无法在不改既有入口的情况下暴露第 6 个主界面视图。

## 本轮实施内容

- 移除 `GpsLoggingService.writeToFile()` 中对 `TrackCacheRepository` 的直接调用。
- 在 `FileLoggerFactory.write()` 入口调用 `TrackCacheRepository.getInstance().append(loc)`，作为新增旁路输出。
- 恢复 `CustomUrlRequest` getter 为包可见，消除该文件相对 `mendhak_master` 的 diff。
- 恢复 `.gitignore` 到 `mendhak_master` 内容，消除无业务意义的 diff。
- 顺手清理历史新增 `vibe` 文档中的 4 处行尾空格，使完整 `git diff --check mendhak_master` 能通过。

## 验证结果

- `git diff --check`：通过。
- `git diff --check mendhak_master`：通过。
- XML/JSON 结构校验：`pref_tracker.xml`、`fragment_track_map_view.xml`、`activity_offline_map_manager.xml`、`strings.xml`、`values-zh-rCN/strings.xml`、`arrays.xml`、三份内置 MapLibre style JSON 均解析通过，输出 `xml/json ok`。
- `./gradlew :gpslogger:assembleDebug`：未能启动，原因是环境没有 Java/JDK：`JAVA_HOME is not set and no 'java' command could be found in your PATH.`。
- `./gradlew :gpslogger:testDebugUnitTest --tests com.mendhak.gpslogger.loggers.customurl.CustomUrlRequestTest`：同样因缺少 Java/JDK 未能启动。

## 已确认 / 待确认 / 不可推断

- 已确认：最终相对 `mendhak_master` 不再修改 `.gitignore` 和 `CustomUrlRequest.java`。
- 已确认：轨迹缓存写入已从服务主流程移动到文件写入入口层，且仍早于 `Session.clearDescription()`。
- 已确认：保留的既有文件改动均能对应到当前硬编码入口、事件、会话状态、依赖或资源数据源。
- 待确认：缺少 JDK，本环境无法完成 Android 编译和单元测试。
- 不可推断：MapLibre 真机渲染、离线缓存和第三方瓦片服务策略，需要在有 Android 构建环境与设备/模拟器后验证。

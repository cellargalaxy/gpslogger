# 260619-155707 执行记录

## 依据

- 任务来源：`vibe/2606p1/260619-155707-prompt.md`，要求「轨迹地图在加载地图瓦片时提示瓦片来自缓存还是在线下载」和「地图中的当前位置点增加当前手机的方向指向」。
- 轨迹地图主实现：`gpslogger/src/main/java/com/mendhak/gpslogger/tracker/ui/TrackMapViewFragment.java`，已有 MapLibre `MapView`、状态浮层、当前定位圆点、轨迹线和图例。
- 当前定位数据来源：`TrackMapViewFragment#getCurrentLocation()` 读取 `Session.getInstance().getCurrentLocationInfo()`；`GpsDetailedViewFragment` 已用 `Location.hasBearing()/getBearing()` 展示方向；`TrackCacheRepository#fromLocation()` 也只在 `loc.hasBearing()` 时保存 `bearing` 字段。
- 瓦片 API 依据：MapLibre Android SDK 11.5.2 sources 中 `MapView.addOnTileActionListener(...)` 回调提供 `TileOperation op, x, y, z, wrap, overscaledZ, sourceID`；`TileOperation` 枚举包含 `RequestedFromCache`、`RequestedFromNetwork`、`LoadFromCache`、`LoadFromNetwork`。
- 不可推断：代码中没有独立的手机罗盘/姿态传感器链路；本次方向指向采用当前 `Location` 的 bearing，不新增传感器权限和生命周期。

## 方案

- 瓦片来源提示：
  - 在 `fragment_track_map_view.xml` 增加独立的 `track_map_tile_source_status` 浮层，避免覆盖原有无轨迹/无底图/加载慢状态。
  - 在 `TrackMapViewFragment` 注册 `addOnTileActionListener`，按 `TileOperation` 显示「Loading map tiles from cache...」「Downloading map tiles...」「Map tiles loaded from cache.」「Map tiles downloaded online.」。
  - 过滤本页自绘 GeoJSON 源：`track_segment_source_*` 和 `track_current_location_source`，只统计底图 source。
  - 在 style 重新加载、fallback、相机变化时重置统计；在 idle 或 fully rendered 时用本轮计数给出缓存/在线/混合摘要，短暂显示后自动隐藏。
- 当前位置方向：
  - 继续复用当前定位 GeoJSON source。
  - 当前点绘制顺序改为光晕、可选方向箭头、中心圆点。
  - 方向箭头使用代码生成的 bitmap 注册到 MapLibre style，通过 `SymbolLayer` 的 `iconRotate(normalizeBearing(loc.getBearing()))` 和 `ICON_ROTATION_ALIGNMENT_MAP` 指向 bearing。
  - `Location` 无 bearing 时只显示原有圆点，不显示方向箭头。

## 实施

- 修改 `gpslogger/src/main/java/com/mendhak/gpslogger/tracker/ui/TrackMapViewFragment.java`
  - 新增 `TileOperation` 监听和瓦片来源统计/提示方法。
  - 新增当前位置方向箭头 bitmap、`SymbolLayer` 和 bearing 归一化逻辑。
- 修改 `gpslogger/src/main/res/layout/fragment_track_map_view.xml`
  - 增加 `track_map_tile_source_status` 顶部浮层。
- 修改 `gpslogger/src/main/res/values/strings.xml`
  - 增加瓦片缓存/在线/混合来源提示文案。

## 验证

- 已执行 XML 解析校验：
  - `fragment_track_map_view.xml` 通过。
  - `values/strings.xml` 通过。
- 已尝试执行 `./gradlew :gpslogger:assembleDebug`，但当前环境无 JDK：
  - `JAVA_HOME is not set and no 'java' command could be found in your PATH.`
  - `command -v java` 无输出，`/usr/lib/jvm` 不存在。
- 因环境缺少 Java，未能完成 Gradle 编译验证。基于 MapLibre 11.5.2 sources 已确认本次使用的 `MapView.addOnTileActionListener`、`TileOperation`、`Style.getImage/addImage`、`SymbolLayer` 和 `PropertyFactory.iconRotate` API 存在。
